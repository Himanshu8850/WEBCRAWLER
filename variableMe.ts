let hero: string;
//ahh so when we are not declaring it
//we need to define the type

function getHero() {
  return "thor";
  //here function should be restricted
  //by what it will return
}

hero = getHero();
